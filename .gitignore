node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
coverage/
test-results/
.worktrees/
*.tsbuildinfo

# Superpowers 实现计划 — 本地 agent 工作流，不纳入公开仓库
docs/superpowers/plans/

# Cursor IDE 本地配置与 hooks 状态
.cursor/

# Codex 本地运行产物（误落在仓库根目录时不纳入 Git）
.codex-runs/

# 本机私有 agent 补充规则，不进入公开仓库
AGENTS.local.md

# 仓库内 builtin 预设数据 — 运行时从 ~/.oc-switch/presets/builtin/ 读取
presets/builtin/

# 本地视觉验证截图等评审产物
output/
