# Presets

预设 JSON **不随仓库分发**，也不应包含任何个人的 Provider 配置样本。

运行时若使用预设，数据来自本机 `~/.oc-switch/presets/custom/`（由 `import` / `presets export` 从当前配置生成）。

日常添加 Provider 请用 **`provider add-custom`** 或 Web Providers 页的自定义添加，无需仓库内置模板。
